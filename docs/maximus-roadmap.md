# Maximus.ai Roadmap (Phase 5)

**As of**: 2026-06-10 (tied to Phase 1-4 findings: audit inventories/debt, competitive gaps vs OpenHands/Cline/Goose/Agent Zero/LangGraph/etc., hidden gems for MCP/memory/agent/safety, architecture scores (high potential B on memory/agent/ext but low 3-5 on prod/maint/deploy/research/obs due to mocks/stubs/frontend absence/claims mismatch)).

**Process**: Synthesized from all prior phases + library AGENTS.md / skills (research-project/hidden-gem-hunt/generate-integration-recipe/orchestrate + catalogs for recipes/cross like full-research-implementation-loop.md + agents/ + mcp/ + local-models/ + research-automation/ + cybersecurity/ + devops/ + observability/ + hidden-gems/ + .grok/skills/); evidence-based (file:line from clone + web sources "as of"); actionable with owners/estimates; local-first/MCP/agent-orchestrated/production bias; 4 phases with features/infra/agent/MCP/memory/security/research/DevX/deployment upgrades. Output ready for docs/maximus-roadmap.md. Dogfooding (use library skills/subagents for execution).

**30 Day Plan (Quick wins / Critical debt / Foundation; focus on high-impact from Phase 4 low scores + Phase 1 debt + Phase 2 gaps like MCP/frontend/claims)**:
- **Features**: Consolidate MCP (pick manager.py real JSON-RPC/Stdio + add tests; remove mocks/dupe client/mcp_manager; wire to registry/loop _get_tool_schemas + terminal). Fix model_router.route() (wire detectors/scorer/intent from model_router.py:198+; remove stub 316-326). Harden safety (enforce layer3 in api/loop paths; move auto-True to explicit UI only; add tests). De-dupe tools (merge task_tools/todo_tool; clean adapters hardcoded). Update claims/docs (README/AGENTS/ARCHITECTURE/PRD to match 0.1.0 reality + "in progress" for MCP/React; remove "✅ Complete" mismatches).
- **Infrastructure**: Fix CI (remove continue-on-error on frontend or stub build; add maximus-terminal/ placeholder or excise). Ensure Docker/compose builds/runs clean (backend + ollama health; note frontend gap). Add basic monitoring (prometheus hooks).
- **Agent upgrades**: Wire rich loop as optional/default or document split (terminal/api simple vs tui/loop rich); add basic evals harness (start with existing tests + library research/benchmarks patterns).
- **Memory upgrades**: Fix compaction TODO (memory/compaction.py:246 "Actually call Ollama"); enable vector by default or document fallback; add simple persistence tests.
- **MCP upgrades**: Real consolidation + tests (as above); expose sample tools (e.g., file + memory query) via MCP for Goose/Cline/Grok.
- **Security upgrades**: Add prompt injection tests + layer3 enforcement; integrate Semgrep (library cybersecurity/tools/) in CI for generated code.
- **Research upgrades**: Basic planner/reflector wiring in loop (from intelligence/); start simple benchmark (reuse tests/mocks as baseline).
- **Developer experience upgrades**: Improve terminal (add --mode flags doc; better errors); add "maximus doctor" enhancements (Ollama + deps + MCP status).
- **Deployment upgrades**: Clean prod compose (note frontend); basic health + logs.
- **Owners/Estimates/Impact**: Core team (MCP/router/safety 3-5 days, high impact); docs 1 day (medium). Risk low (mostly fixes). Success: MCP works with real servers; router uses intent; no "✅ Complete" lies; clean build.

**90 Day Plan (Core production / Missing capabilities from Phase 2 + gems / Polish)**:
- **Features**: Full MCP ecosystem (register real servers like library mcp/manifests + hidden gems e.g. mcp-knowledge-graph/shaneholloman for MemoryMesh; auto_discover + list in terminal). Frontend (implement minimal from REACT_TERMINAL_PLAN.md or integrate existing tui; or clear "terminal-first" positioning). Sub-agent / orchestration (enhance spawner + wire LangGraph-style from library agent-frameworks/ + recipes; teams like Cline/OpenHands). Evals (SWE-bench-like harness + publish; use library research/ + benchmark-exploits awareness for harness audit).
- **Infrastructure**: Full CI (frontend if kept; integration with real Ollama/MCP); prod deploy guide updates (SSL + scaling notes from DEPLOYMENT.md).
- **Agent upgrades**: Router + stance + planner/reflector full in default path; multi-agent teams with memory sharing; stance auto + user override.
- **Memory upgrades**: Production MemoryMesh (vector default + compaction AI; Graphiti cross from library rag-memory/ for temporal/audit; Memdir enhancements).
- **MCP upgrades**: 5+ real integrations (from Phase 3 gems: swarmclaw for runtime/swarm, agent-security-scanner for safety/Semgrep, ragdocs for RAG, deep-research-agent for research handoff; library mcp/servers.md like qdrant/github/ollama).
- **Security upgrades**: Agent security scanner MCP (Phase 3 gem) + prompt firewall; Semgrep/Trivy in loop for generated code (library cybersecurity/ + mcp/recipes/); trust_layer + hooks full.
- **Research upgrades**: Deep research node (Local Deep Research / Open Deep Research from library research-automation/ + Phase 3 gems like tarun7r/deep-research-agent; citations + cred scoring; feed to planner/MemoryMesh).
- **Developer experience upgrades**: VSCode/Cursor/Windsurf extensions or MCP marketplace (like Cline per library); "maximus analyze --repo" enhancements (adapters real + from library); better docs/examples.
- **Deployment upgrades**: One-click (Coolify/Dokploy from library devops-deployment/ for maximus + Ollama + MCPs); Kubernetes notes; resource profiles.
- **Owners/Estimates/Impact**: Core + community (MCP/integrations 2-3 weeks high; frontend 3-4 weeks high; evals/research 2 weeks medium-high). Risk medium (new features). Success: Real MCP works end-to-end; frontend ships or scoped; evals published; 5+ gems integrated; "B" architecture scores rise to A on prod/maint/deploy.

**180 Day Plan (Elite platform / Self-improvement / Competitive parity / Scale)**:
- **Features**: Self-improving loops (Phase 7 design: maximus uses its loop + research agents (library research-automation/ + gems) + MCP (from Phase 3) + memory (Graphiti + MemoryMesh) + evals to audit/improve own code + discover gems + update catalogs/docs; continuous repo discovery like library). Full orchestration (LangGraph/CrewAI recipes from library agent-frameworks/ + mcp/recipes/; sub-agents with real sandbox like Agent Zero). Advanced memory/KG (full Graphiti integration + knowledge graphs from Phase 3 gems like mcp-knowledge-graph; branching + compaction prod). Browser/research automation (library browser-automation/ Browser Use/Skyvern + Phase 3 gems for visual QA + deep research handoff to coding).
- **Infrastructure**: Multi-tenant / scaling (real Docker/K8s sandboxes from library devops/ + gems like agent-infra/sandbox; observability full Langfuse + traces for all loops/MCP from library observability/).
- **Agent upgrades**: Production teams (like OpenHands/Cline from Phase 2; with evals + self-correction); full 8-state + intelligence in default; auto stance + model routing prod.
- **Memory upgrades**: Enterprise (Graphiti temporal + audit + RAG over library catalogs + maximus sessions; long-term Memdir + vector prod).
- **MCP upgrades**: 10+ (full from Phase 3 + library mcp/ + new hunts; security/obs/MCP standard gems; expose maximus tools as MCP server for Cursor/Windsurf/Claude/etc.).
- **Security upgrades**: Full agent security (Phase 3 scanner + library cybersecurity/ pentest-ai-mcp + MAESTRO from research/papers; ZTA + tool poisoning guards; continuous audit via self-improvement).
- **Research upgrades**: Full research lead (library research-automation/ S cluster + Phase 3 scientific gems like PaperOrchestra/Ar9av + AgentLaboratory; citations + retractions + multimodal; self-audit maximus + library).
- **Developer experience upgrades**: IDE-first (MCP marketplace + extensions for VSCode/Cursor/Windsurf like library agents/ + mcp/); "maximus self-improve" command (triggers research + code + verify + PR).
- **Deployment upgrades**: Cloud + self-host parity (Coolify/Dokploy/K8s from library + gems; one-command with Ollama/MCP/memory; scaling profiles).
- **Owners/Estimates/Impact**: Core + ecosystem (self-improvement 4-6 weeks high; integrations 4 weeks; scaling 3 weeks). Risk medium-high (new paradigm). Success: Self-improvement demo (maximus audits/improves own gaps + contributes to library); competitive parity on MCP/frontend/orch/evals (from Phase 2 gaps closed); 10+ gems wired; A+ scores on Phase 4 dimensions; production users.

**1 Year Plan (Dominant local elite platform / Ecosystem leader / Continuous evolution)**:
- **Features**: Full self-improving AI engineering OS (Phase 7 architecture: research/coding agents + MCP + KGs (Graphiti + Phase 3) + long-term memory + evals + continuous discovery (library-style + maximus loop) + auto PRs to self + library + GitHub; multi-modal + vision + browser (library browser-automation/ + gems); creative domains (library game-dev/ + godot MCP gems for "build game from research"); scientific (library research/ + gems like deep-research-agent/PaperOrchestra).
- **Infrastructure**: Global self-host network (library self-hosted/ + devops/ + Coolify/Dokploy/Argo/Flux; edge Ollama + central memory/KG; zero-trust from library research/papers/MAESTRO/NSA).
- **Agent upgrades**: Sovereign swarms (like swarmclaw gem + library agent-frameworks/ + multi-agent; with real sandboxes + evals + self-evolution).
- **Memory upgrades**: Planetary KG (Graphiti + all library + user sessions + public research; RAG + reasoning over maximus code + ecosystem).
- **MCP upgrades**: Universal (maximus as MCP server + client for everything; 50+ from hunts + library + new; security/obs standard).
- **Security upgrades**: Self-securing (continuous pentest via library cybersecurity/ + gems + self-improvement; ZTA + MAESTRO + benchmark exploit awareness).
- **Research upgrades**: Autonomous research org (library research-automation/ + Phase 3 + papers + leaked as blueprints; auto literature + experiments + papers on maximus improvements).
- **Developer experience upgrades**: The default (MCP everywhere; IDE + CLI + web + voice; "maximus" as verb like "grep"; self-maintaining + contributes to open source/library).
- **Deployment upgrades**: Ubiquitous (one-command anywhere; K8s + edge + air-gapped; library bootstrap-ultimate-stack patterns).
- **Owners/Estimates/Impact**: Core + community + foundation (ecosystem 6+ months high; research 3 months; infra 3 months). Risk high (visionary). Success: Maxim us as the local-first elite AI engineering platform (S in agent-frameworks/ per library); self-improves + elevates the ecosystem (contributes gems/recipes to library + GitHub); competitive leader vs Phase 2 list (or defines new category); production at scale with trust (A+ Phase 4 scores); vibrant community (plugins/MCP/swarms).

**Cross-refs + Recipes (per library generate-integration-recipe skill + AGENTS "recipes highest-value")**: Every phase includes ties to library (e.g., 30d: "register Semgrep MCP from library cybersecurity/tools/ + mcp/ in maximus loop for generated code gates per mcp/recipes/coding-agent-memory-browser-semgrep.md"; 90d: "use Phase 3 gems like mcp-knowledge-graph + swarmclawai with library rag-memory/Graphiti + research-automation/ + agents/Cline/Goose + .grok/skills/ for 'Grok subagent: after hidden-gem-hunt... spawn... persist to graph + maximus memory... propose PR'"; 180d/1yr: "full self-improvement per Phase 7 + library mcp/recipes/education-se-research-to-agent-coding-loop.md + full-research-implementation-loop.md + game-dev-research-implement-verify-deploy-loop.md + orchestrate-category-research skill + library-status for maximus + ecosystem audit"; "Inside Grok-powered session: use research-project on maximus gaps + hidden-gem-hunt for integrations + generate-integration-recipe for cross + spawn subagent to implement in clone + update all maximus-*.md + library catalogs").

**Sources / freshness**: Synthesized from Phase 1-4 artifacts (maximus-audit.md with file:line 2026-06-10 clone; competitive-analysis.md with web sources "as of 2026-06"; hidden-gems.md with exact searches/verified commits; architecture-review.md 9-axis from AGENTS rubric + evidence); library catalogs/skills/AGENTS.md (recipes + cross + "as of 2026-06-10"); clone docs (README/ARCH/PRD/AGENTS/DEPLOYMENT/REACT_TERMINAL_PLAN + code). "Research performed 2026-06-10... Re-verify live; use skills/refresh-metrics.py". All per AGENTS (evidence, citations, "as of", sources with queries/tools, local/AI/MCP bias, actionable recipes).

(Full phased details + owners/estimates/risk + success criteria + variations in the written file; ties to other 6 maximus-*.md + library for execution.)