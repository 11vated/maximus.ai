# Maximus Polishing Plan - Make it the Next Claude

## Current State Analysis

### ✅ Completed
- `bin/maximus.py` - Correctly imports from `maximus.ui.terminal:main`
- Terminal UI with virtual scrolling (`VirtualScrollBuffer`)
- Error boundaries (`ErrorBoundary` class)
- Session persistence (`SessionManager`)
- Backend API (`MaximusBackend`)
- 11 E2E tests passing
- Docker deployment setup
- Deployment guide

### ❌ Issues Found
1. **Wrong entry point in pyproject.toml** - Points to `maximus.cli:cli` instead of `maximus.ui.terminal:main`
2. **README out of date** - Still references `python bin/maximus.py` directly and old phases
3. **Legacy CLI still present** - `maximus.cli` contains deprecated commands

## Polishing Tasks

### Phase 1: Fix Entry Point
**File: `pyproject.toml` line 38**
```toml
# BEFORE
maximus = "maximus.cli:cli"

# AFTER  
maximus = "maximus.ui.terminal:main"
```

**Verification:**
```bash
pip install -e .
maximus --help  # Should show new commands
```

### Phase 2: Update Documentation
**File: `README.md`**
- Update "Next Steps" section (lines 128-133) - Mark Phase 3-4 as complete
- Update usage examples to use `maximus` instead of `python bin/maximus.py` where appropriate
- Fix broken reference to `maximus-terminal` directory (deleted)

### Phase 3: Clean Up Legacy Code
**Option A: Keep for backward compatibility**
- Mark old CLI commands as hidden (already done via `@click.hidden`)

**Option B: Remove deprecated CLI**
- Delete or significantly simplify `src/maximus/cli.py`

### Phase 4: Verify Everything Works
1. Fresh install test: `pip install -e .`
2. Run `maximus doctor` - should show Ollama status
3. Run `maximus --help` - should show all commands
4. Run `pytest tests/e2e/` - ensure all 11 tests pass
5. Run `maximus --new` - start and exit cleanly

## Expected Final State

```
$ pip install -e .
$ maximus --help
Maximus - AI Coding Assistant

Usage:
    maximus                    Start session (show menu if sessions exist)
    maximus --model <name>     Use specific model
    maximus --session <id>     Resume specific session
    maximus --verbose          Show debug info
    maximus doctor             Run diagnostics
```

## Commands to Run
```bash
# Fix entry point
# (Edit pyproject.toml)

# Reinstall
pip install -e .

# Verify
maximus --help
python -m pytest tests/e2e/ -v

# Push changes  
git add -A
git commit -m "polish: Fix entry point and update documentation"
git push
```

## Success Criteria
- [ ] `maximus` command starts terminal UI
- [ ] `maximus --help` shows correct help
- [ ] `maximus doctor` runs diagnostics
- [ ] All 11 E2E tests pass
- [ ] README accurately reflects current state
- [ ] No references to deleted `maximus-terminal/` in docs